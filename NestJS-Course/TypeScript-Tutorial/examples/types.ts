// if tries to use a method that the specific type does not have, an error will be occured
const today = new Date();
today.getMonth();

/////////////////////////////////////
const person = {
  age: 20,
};

/////////////////////////////////////
class Color {}
const red = new Color();
