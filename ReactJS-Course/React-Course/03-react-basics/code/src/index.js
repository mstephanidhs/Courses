import ReactDOM from "react-dom/client";
import React from "react";

import "./index.css";
// Basically it's App.js (but .js can be omitted)
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
